<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />

    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}/}spring.log}"/>
    <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${PID:- } [%t] --- %-40.40logger{39} : %m%n"/>
	<property name="SESSION_LOG_FILE" value="${session.log:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}/}session.log}"/>

    <appender name="FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%i</fileNamePattern>
        </rollingPolicy>
        <triggeringPolicy
                class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>10MB</MaxFileSize>
        </triggeringPolicy>
    </appender>

    <appender name="SESSION-LOG" class="ch.qos.logback.core.FileAppender">
        <file>${SESSION_LOG_FILE}</file>
        <append>true</append>
        <encoder>
            <pattern>%date{ISO8601} %msg %n</pattern>
        </encoder>
    </appender>
    
    <!-- additivity=false ensures session data only goes to the session log -->
    <logger name="session" level="INFO" additivity="false">
        <appender-ref ref="SESSION-LOG"/>
    </logger>
    
    <root level="INFO">
        <appender-ref ref="FILE" />
    </root>

</configuration>
